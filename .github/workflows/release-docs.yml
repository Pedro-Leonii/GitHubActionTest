name: Rilascio documentazione compilata

on: 
    push:
        branches:
            - main
jobs:
    publish:
        runs-on: ubuntu-latest

        steps:
        - name: checkout source
          uses: actions/checkout@v4
            
        - name: check out target
          uses: actions/checkout@v4
          with: 
            repository: PedroLeonii/prova
            token: ${{ secrets.PAT_PROVA }}

        - name: echo
          run: tree 